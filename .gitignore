# Dependencies
  node_modules/
  */node_modules/

  # Build outputs
  build/
  dist/
  .next/

  # Environment files
  .env
  .env.local
  .env.production
  .env.test

  # Logs
  *.log
  logs/

  # OS generated files
  .DS_Store
  Thumbs.db

  # IDE files
  .vscode/
  .idea/

  # Hardhat files
  cache/
  artifacts/
  deployments/localhost/

  # Coverage
  coverage/
  
  # Dependencies
  node_modules/
  */node_modules/
  **/node_modules/

  # Build files
  cache/
  artifacts/
  build/
  dist/

  # Environment files - CRITICAL SECURITY
  .env*
  *.env*
  backend/.env*
  frontend/.env*
  **/.env*

  # Hardhat/Blockchain
  deployments/localhost/
  hardhat-node.log
  
  # CRITICAL SECURITY - NEVER COMMIT THESE
  **/*private*
  **/*secret*
  **/*webhook*
  **/*key*
  **/.env*
  **/logs/
  **/*.log
  
  # Discord and Webhook URLs
  *webhook*
  *discord*
  
  # Backup and temp files
  backup*/
  temp/
  tmp/
  *.backup
  *.bak
  
  # IDE and editor files
  .claude/
  **/.claude/
  .vscode/settings.json
  
  # Security sensitive directories
  backend/data/backups/
  backend/security/secure-wallets/
  
  # ULTRA-SECURITY PATTERNS - NEVER COMMIT THESE
  # Private keys and mnemonics (any format)
  *private*key*
  *mnemonic*
  *seed*phrase*
  *.pem
  *.p12
  *.pfx
  
  # API keys and tokens
  *api*key*
  *secret*key*
  *access*token*
  *auth*token*
  *bearer*token*
  *jwt*token*
  
  # Webhook URLs and endpoints
  *webhook*url*
  *webhook*endpoint*
  *discord*webhook*
  *telegram*webhook*
  
  # Backup and temporary files with potential secrets
  *.backup
  *.bak
  *.tmp
  *.temp
  *backup*
  *temp*
  *temporary*
  
  # Logs that might contain sensitive data
  *.log
  *log*
  logs/
  **/logs/
  
  # Configuration files that might have secrets
  config.json
  config.yml
  config.yaml
  *config*.json
  *config*.yml
  *config*.yaml
  
  # Database exports and dumps
  *.sql
  *.dump
  *.backup
  
  # Any file with 'secret' or 'private' in name
  *secret*
  *private*
  *credential*
  *password*